export const planeSize = 1000;
export const leftBound = -56;
export const rightBound = 56;

// The only asset left to preload: everything else in the scene is generated
// geometry now, so there is nothing to download for it.
export const MODEL_PATHS = ['/models/player/scene.gltf'];
